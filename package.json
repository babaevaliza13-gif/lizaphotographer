{
  "name": "liza-babaieva-editorial",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "content": "node scripts/build-content.mjs",
    "predev": "npm run content",
    "prebuild": "npm run content",
    "prepreview": "npm run content",
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1"
  },
  "devDependencies": {
    "vercel": "^54.14.5",
    "vite": "^5.4.11"
  }
}
